<%= form_for [@project, @requirement] do |f| %> 

	<%= f.hidden_field :parent_id %>	


	<p>
		<%= f.label :name, "Requirement name" %>
		<%= f.text_field :name %> 

		<%= f.label :description %>
		<%= f.text_area :description %> 
	</p>
	<p>
		<%= f.submit "Create Requirement"%> 
	</p>

<% end %>